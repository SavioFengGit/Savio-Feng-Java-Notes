package MathClass;

public class Main {

	public static void main(String[] args) {

		double x = 3.14;
		double y = -10;
		double abs= Math.abs(y);
		double z = Math.cos(y);
		double max = Math.max(x, y);
		System.out.println("The cos(y) it's " + z);
		System.out.println("The max between x and y is...    " + max);
		System.out.println("The abs(y) is...    " + abs);
	}

}
